<h1><strong>New review</strong></h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
<%= f.input :content %>
<%= f.input :rating %>
<%= f.submit %>
<% end %>

<%= link_to "all reviews", restaurant_reviews(@restaurant.id)  %>

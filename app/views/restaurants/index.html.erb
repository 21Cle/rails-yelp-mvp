<div class="container">
  <h1><strong> All Restaurants </strong></h1>
  <ul>
    <% @restaurants.each do |restaurant|  %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %> - <%= link_to "🗑", restaurant_path(restaurant), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
    <% end %>
  </ul>
</div>

<%= link_to "+ Add a restaurant", new_restaurant_path %>
